#!/usr/bin/env python3.5

import json

import blessed

import timber.log_formatter
import timber.logging_service
import timber.ui


t = blessed.Terminal()

if __name__ == '__main__':
    timber.ui.ui_event_loop(t, None, None)
    # logs = timber.logging_service.fetch_latest_logs()
    # for log in logs.get('entries', []):
    #     print(timber.log_formatter.format(log))
